from openpyxl import load_workbook
import os 
here=os.path.dirname(__file__) 
tdir=os.path.join(here,'templates') 
def clear_detail(p): 
  wb=load_workbook(p); ws=wb.active 
  for r in range(2, ws.max_row+1): 
    for c in range(1, ws.max_column+1): ws.cell(r,c).value=None 
  wb.save(p) 
def clear_summary(p): 
  wb=load_workbook(p); ws=wb.active 
  for r in range(4, ws.max_row+1): 
    for c in range(1, ws.max_column+1): ws.cell(r,c).value=None 
  # ??? ????? ????? ?????? ?? ?????? ?????? ?????? ??? ??? ?????? 
  tasks=['????? ?????','????? ?????','????? ?????','????? ??????','?????? ?????','?????','?????? ??????','????? ???????','??? ??????','????? ??????','????? ???','????','????'] 
  r=4 
  for t in tasks: ws.cell(r,2).value=t; r+=1 
  wb.save(p) 
def clear_spares(p): 
  wb=load_workbook(p); ws=wb.active 
  for r in range(4, ws.max_row+1): 
    for c in range(1, ws.max_column+1): ws.cell(r,c).value=None 
  wb.save(p) 
if __name__=='__main__': 
  d=os.path.join(tdir,'detail.xlsx'); s=os.path.join(tdir,'summary.xlsx'); sp=os.path.join(tdir,'spares.xlsx') 
  if os.path.exists(d): clear_detail(d); print('detail cleared') 
  if os.path.exists(s): clear_summary(s); print('summary cleared') 
  if os.path.exists(sp): clear_spares(sp); print('spares cleared') 
